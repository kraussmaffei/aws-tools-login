{
  "name": "aws-tools-login",
  "version": "1.0.0",
  "description": "Log into several aws tools. (e.g. codeartifact, ecr ...).",
  "main": "index.js",
  "scripts": {
    "lint": "eslint **.js",
    "prepare": "ncc build index.js -o dist --license licenses.txt && ncc build cleanup.js -o dist/cleanup",
    "all": "npm run lint && npm run prepare"
  },
  "author": "Kraussmaffei Technologies",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.1.1",
    "@aws-sdk/client-codeartifact": "^3.325.0",
    "aws-sdk": "^2.1370.0"
  },
  "devDependencies": {
    "@vercel/ncc": "0.36.1",
    "eslint": "^8.39.0"
  }
}
